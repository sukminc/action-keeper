export default function ContractIndexPage() {
  return (
    <main>
      <header className="hero" style={{ marginBottom: "1.2rem" }}>
        <span className="hero-badge">Shared Contract</span>
        <h1 className="hero-title">Contract ID is required.</h1>
        <p className="hero-subtitle">
          Open a link like <code>/contract/your-agreement-id</code> from Seller or Buyer page.
        </p>
      </header>
    </main>
  );
}
